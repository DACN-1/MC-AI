# Python 3.10 VLA Action Head Training Requirements
# Compatible with Python 3.10.4+ on JURECA DC cluster

# Core ML libraries - CUDA 11.7 compatible versions
torch==2.1.0
torchvision==0.16.0
torchaudio==2.1.0

# Computer vision and image processing
opencv-python>=4.5.0
Pillow>=9.0.0

# Transformers and LLaVA - Python 3.10 compatible (pinned for torch 2.1.0)
transformers>=4.37.0,<4.45.0
accelerate>=0.20.0,<0.35.0
sentencepiece>=0.1.97

# Data processing and utilities - capped at <1.24 for minerl compatibility
numpy>=1.16.2,<1.24
tqdm>=4.64.0
scikit-learn>=1.1.0
matplotlib>=3.5.0
h5py>=3.8.0  # only used by the deprecated chunk_frames.py — safe to drop once that file is removed
decord>=0.6.0  # MP4 random-access frame decoding for TrajectoryDataset

# Additional scripts dependencies
seaborn>=0.11.0  # For enhanced plotting in evaluate_model.py

# MineRL and game environment
# gym 0.21.0 has a broken setup.py; 0.23.1 is the highest version minerl v1.0 supports
gym==0.23.1
gym3>=0.3.3
attrs>=21.4.0
# minerl is installed from git inside the Dockerfile (v1.0)

# Additional utilities
coloredlogs>=15.0

# Built-in modules (no installation needed)
# json - Python 3.10 built-in
# argparse - Python 3.10 built-in  
# pathlib - Python 3.10 built-in (pathlib2 not needed)
# logging - Python 3.10 built-in
# shutil - Python 3.10 built-in
# time - Python 3.10 built-in